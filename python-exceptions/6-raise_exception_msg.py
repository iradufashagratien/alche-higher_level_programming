#!/usr/bin/python3
"""Module that raises a name exception with a custom message."""


def raise_exception_msg(message=""):
    """Raise a NameError with the given message."""
    raise NameError(message)
