# SQL_introduction

Project covering basic MySQL commands: creating/dropping databases,
creating tables, inserting, updating, deleting and querying data.

Environment: Ubuntu 20.04 LTS, MySQL 8.0.
Each script is run like this:

    cat N-script.sql | mysql -uroot -p [database_name]
